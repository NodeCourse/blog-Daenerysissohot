html
    head
        title Exo4 jerem.v
        link(rel="stylesheet",href="styles.css")

    body
        ul
            h2 Jerem.v Website


            for post in posts
                article
                h1 #{post.title}

                p si tu kiff met un poce ble ma couille ! :
                form(method="post", action="/api/post/" + post.id + "/upvote")
                    button(type="submit") upvote

                form(method="post", action="/api/post/" + post.id + "/downvote")
                    button(type="submit") downvote
                p Rajouter un post :
                form(method="post", action="/api/post/")
                    input(type="text", name="title", placeholder="Post Title ...")
                    button(type="submit") Post




